export enum POST_STATUS {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
}

export enum COMMENT_STATUS {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
}

export enum ORDER_BY {
  ASC = 'ASC',
  DESC = 'desc',
}
